package sort;

import heap.Heap;

public class HeapSort implements Sort {

    @Override
    public int[] sort(int[] arr) {
        Heap<Integer> heap = new Heap<>(arr.length);

        return new int[0];
    }
}
